# version is obsolete
# version: "3"

networks:
  proxy-tier:

services:

  proxy:
    image: tozd/nginx-proxy:ubuntu-bionic
    container_name: "nginx-proxy"
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /etc/localtime:/etc/localtime:ro
      - /var/docker/nginx-proxy/ssl:/ssl:rw
      - /var/docker/nginx-proxy/var/log/nullmailer:/var/log/nullmailer:rw
      - /var/docker/nginx-proxy/var/log/unattended-upgrades:/var/log/unattended-upgrades:rw
      - /var/docker/nginx-proxy/var/log/dockergen:/var/log/dockergen:rw
      - /var/docker/nginx-proxy/var/log/dnsmasq:/var/log/dnsmasq:rw
      - /var/docker/nginx-proxy/var/log/letsencrypt:/var/log/letsencrypt:rw
      - /var/docker/nginx-proxy/var/log/nginx:/var/log/nginx:rw
    environment:
      - LETSENCRYPT_EMAIL=admin@kubux.net
    networks:
      - proxy-tier


  whiteboard:
    build:
      context: "whiteboard.kubux.net/kubux-whiteboard-server"
      dockerfile: "Dockerfile"
    container_name: "whiteboard.kubux.net"
    hostname: "whiteboard.kubux.net"
    depends_on:
      - proxy
    expose:
      - 80
    volumes:
      - /var/docker/whiteboard.kubux.net/kubux-whiteboard-server:/app:rw
      - /etc/localtime:/etc/localtime:ro
    environment:
      - VIRTUAL_HOST=whiteboard.kubux.net
      - VIRTUAL_URL=/
      - VIRTUAL_PORT=80
      - VIRTUAL_PROTO=http
      - VIRTUAL_LETSENCRYPT=whiteboard.kubux.net
      - LETSENCRYPT_HOST=whiteboard.kubux.net
    networks:
      - proxy-tier
    restart: unless-stopped

# end of file
